# permissions for end users to view mailboxes.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: mailcow-operator
    app.kubernetes.io/managed-by: kustomize
  name: mailbox-viewer-role
rules:
- apiGroups:
  - mailcow.onestein.nl
  resources:
  - mailboxes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - mailcow.onestein.nl
  resources:
  - mailboxes/status
  verbs:
  - get
